#include "main.h"

/**
 * _isupper - function for uppercase character
 * @c: integer
 * Return:0 (sucess)
*/

int _isupper(int c)
{
	int i;

	for (i = 'A' ; i <= 'Z' ; i++)
	{
		if (c == i)
		{
			return (0);
		}
		else
		{
			return (0);
		}
	}
return (0);
}
