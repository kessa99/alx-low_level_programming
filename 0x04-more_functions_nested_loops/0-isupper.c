#include "main.h"

/**
 * _isupper - print if is upper or not
 * @c: integer for the entry
 * Return: 1 for sucess 0 for failed
*/

int _isupper(int c)
{
	if ((c >= 'A')&&(c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
